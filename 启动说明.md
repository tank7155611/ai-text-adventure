# AI 文字冒险本地 Demo 启动说明

## 1. 环境要求

本项目是 Vite + React + TypeScript 本地 Demo。

建议环境：

```bash
node -v
npm -v
```

当前本机可用版本：

```text
Node.js v24.12.0
npm 11.6.2
```

请使用 Node.js 24 或更高版本。

## 2. 项目目录

进入项目根目录：

```bash
cd <你的项目目录>
```

根目录下应能看到：

```text
package.json
vite.config.ts
src/
public/
.env.example
```

## 3. 安装依赖

如果是第一次运行，或者删除过 `node_modules`，执行：

```bash
npm ci
```

如果 `node_modules` 已经存在，一般可以跳过这一步。

## 4. 配置接口

项目使用 OpenAI 兼容接口，通过 Vite 本地代理请求模型。

将根目录的 `.env.example` 复制为 `.env.local`，填写自己的配置（下方地址和模型仅为占位符）：

```env
VITE_OPENAI_COMPAT_BASE_URL=https://api.example.com/v1
VITE_OPENAI_COMPAT_MODEL=your_model_name
OPENAI_COMPAT_API_KEY=请填写你的 API Key
```

说明：

- `VITE_OPENAI_COMPAT_BASE_URL`：OpenAI 兼容接口地址。
- `VITE_OPENAI_COMPAT_MODEL`：填写你的服务商支持的模型名称。
- `OPENAI_COMPAT_API_KEY`：接口密钥，只给 Vite 本地代理使用。

注意：

- 不要把真实 API Key 写进文档。
- 不要提交 `.env.local`。
- 当前 `.gitignore` 已忽略 `.env` 和 `.env.*`，只允许提交不含密钥的 `.env.example`。
- 密钥不能使用 `VITE_` 前缀，否则可能进入前端产物。
- 模型调用由你自己的账号计费；不要对外开放带密钥的本地开发服务器。

## 5. 启动开发服务

执行：

```bash
npm run dev
```

启动成功后，终端会显示类似：

```text
Local: http://127.0.0.1:5173/
```

打开浏览器访问：

```text
http://127.0.0.1:5173/
```

如果 `5173` 端口被占用，Vite 可能会自动换到 `5174`、`5175` 等端口，以终端实际显示为准。

## 6. 日常运行流程

一般按这个顺序：

```bash
cd <你的项目目录>
npm run dev
```

然后在浏览器打开终端显示的本地地址。

## 7. 构建检查

如果想检查项目是否能正常打包：

```bash
npm run build
```

构建成功后会生成或更新 `dist/` 目录。

## 8. 预览构建结果

先构建：

```bash
npm run build
```

再预览：

```bash
npm run preview
```

终端会显示一个本地预览地址，打开即可查看打包后的页面。

## 9. 常见问题

### 页面能打开，但 AI 没有响应

优先检查：

1. `.env.local` 是否存在。
2. `OPENAI_COMPAT_API_KEY` 是否填写。
3. `VITE_OPENAI_COMPAT_BASE_URL` 是否正确。
4. 终端是否出现接口请求错误。

修改 `.env.local` 后，需要停止并重新执行：

```bash
npm run dev
```

### 端口打不开

看终端实际输出的地址。Vite 不一定总是 `5173`，如果端口被占用，可能会变成其他端口。

### 依赖异常

可以重新安装依赖：

```bash
npm install
```

`npm ci` 会按锁文件重新安装依赖；请保留 `package-lock.json`，确保依赖版本可复现。

### JSON 生成失败

当前 Demo 会自动修复一次控制 JSON。如果仍失败，页面会保留已生成剧情，并允许重新生成选项。

这种情况通常和模型输出格式有关，不代表项目启动失败。
